#!/bin/bash
# to generate names (20k), run: ./generate-list.sh names
# to generate words (500k), run: ./generate-list.sh words

in="../data/$1.txt"
out='words.js'
echo "// auto-generated by $(basename $0)">$out
echo words = $(jq -R -s -c 'split("\n")' < $in)>>$out